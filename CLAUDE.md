# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

`aws-sqs-manager-desktop` is a desktop application for managing AWS SQS queues. The repository is in its initial state — only a README and LICENSE exist. No framework, build system, or code has been chosen yet.

## Current State

This project has not been bootstrapped. Before implementing features, the following foundational decisions need to be made:

- **Framework**: e.g., Electron, Tauri, or another desktop app framework
- **Language/Runtime**: e.g., TypeScript/Node.js, Rust
- **UI library**: e.g., React, Svelte, Vue
- **AWS SDK**: e.g., `@aws-sdk/client-sqs` (v3)
- **Build tooling**: e.g., Vite, webpack, esbuild

Once the stack is chosen and the project is bootstrapped, update this file with build, lint, and test commands, plus the architecture overview.
